
!!! note
    - **[Configure Rate Limiting for the API Gateway Cluster]({{base_path}}/api-gateway/rate-limiting/configuring-rate-limiting-api-gateway-cluster.md)** so that the request counters will be replicated across the API Gateway cluster when working with multiple API Gateway nodes. 
    - Configuring Rate Limiting for the API Gateway cluster is not applicable if you have a clustered setup.
 